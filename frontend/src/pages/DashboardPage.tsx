import { Empty } from 'antd'

export default function DashboardPage() {
  return (
    <div
      style={{
        display: 'flex',
        alignItems: 'center',
        justifyContent: 'center',
        minHeight: 300,
      }}
    >
      <Empty description="Dashboard sẽ được phát triển sau" />
    </div>
  )
}
